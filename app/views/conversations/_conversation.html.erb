<li class="relative bg-white py-5 px-4 hover:bg-gray-50 focus-within:ring-2 focus-within:ring-inset focus-within:ring-indigo-600">
  <div class="flex justify-between space-x-3">
    <div class="min-w-0 flex-1">
      <%= tag.div id: dom_id(conversation) do %>
        <%= tag.a href: conversation_path(conversation), class: "block focus:outline-none" do %>
          <span class="absolute inset-0" aria-hidden="true"></span>
          <p class="text-sm font-medium text-gray-900 truncate"><%= conversation.subject %></p>
          <% if post = conversation.posts.first %>
            <p class="text-sm text-gray-500 truncate"><%= post.body %></p>
          <% end %>
        <% end %>
      <% end %>
    </div>
    <time datetime="2021-01-27T16:35" class="flex-shrink-0 whitespace-nowrap text-sm text-gray-500">1d ago</time>
  </div>
</li>
